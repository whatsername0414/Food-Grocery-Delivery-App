query Category {
    getCategories {
        id
        name
        img_url
    }
}

query Merchants($category: String!) {
    getMerchantsByCategory(category: $category) {
        id
        name
        img_url
        categories
        rating
        opening
        isOpen
    }
}