query Category {
    getCategories {
        id
        name
        img_url
    }
}

query Merchants($category: String!) {
    getMerchantsByCategory(category: $category) {
        _id
        name
        img_url
        categories
        rates
        ratings
        favorite
        opening
        isOpen
    }
}