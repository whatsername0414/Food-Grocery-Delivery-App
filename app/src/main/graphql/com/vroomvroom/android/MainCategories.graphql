query HomeData($category: String!) {
    getCategories {
        id
        name
        img_url
    }
    getRestaurants(category: $category) {
        id
        name
        img_url
        categories
        rating
        opening
        isOpen
    }
}