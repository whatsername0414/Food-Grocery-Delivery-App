query User {
    getUser {
        id
        name
        email
        phone_number
        location {
            address
            city
            coordinates
        }
    }
}

mutation UpdateUserLocation($location: LocationInput!) {
    updateLocation(location: $location) {
        location {
            address
            city
            coordinates
        }
    }
}